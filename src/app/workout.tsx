import { PendingScreen } from '@/shared/ui';

export default function WorkoutScreen() {
  return (
    <PendingScreen
      title="Modo entrenamiento"
      description="Pantalla completa sin navegación: ejercicio, series, registro en un toque, temporizador, y sustituir / ajustar / preguntar. Wireframe 1 en docs/03."
    />
  );
}
