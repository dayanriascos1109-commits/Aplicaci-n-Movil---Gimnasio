import { PendingScreen } from '@/shared/ui';

export default function ProfileTab() {
  return (
    <PendingScreen
      title="Perfil"
      description="Objetivos, mis lugares y su equipo, preferencias, y los datos de la cuenta: exportación y eliminación."
    />
  );
}
