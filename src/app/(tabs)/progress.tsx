import { PendingScreen } from '@/shared/ui';

export default function ProgressTab() {
  return (
    <PendingScreen
      title="Progreso"
      description="Indicadores adelantados: volumen, constancia, repeticiones añadidas y RPE. Son los que se mueven desde la semana 1."
    />
  );
}
